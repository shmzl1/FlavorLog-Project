# ==========================================
# 1. 核心 Web 框架与服务器
# ==========================================
fastapi==0.115.6
uvicorn[standard]==0.32.1
python-multipart==0.0.19
# 异步文件 IO 支持
aiofiles==24.1.0

# ==========================================
# 2. 数据库与 ORM 映射
# ==========================================
SQLAlchemy==2.0.36
# 同步驱动
psycopg2-binary==2.9.10
# 异步驱动 (推荐未来切换到异步引擎使用)
asyncpg==0.30.0
alembic==1.14.0

# ==========================================
# 3. 配置管理与环境变量
# ==========================================
pydantic[email]==2.10.3
pydantic-settings==2.6.1
python-dotenv==1.0.1
# Pydantic 邮箱格式校验增强
email-validator==2.2.0

# ==========================================
# 4. 安全、认证与加密体系
# ==========================================
passlib[bcrypt]==1.7.4
# ⚠️ 锁定 bcrypt 版本，防止 passlib 内部验证时触发 72 bytes 报错
bcrypt==3.2.2
python-jose[cryptography]==3.3.0

# ==========================================
# 5. 网络请求与外部 API 交互
# ==========================================
requests==2.32.3
httpx==0.28.1

# ==========================================
# 6. AI 算法与多模态数据处理
# ==========================================
numpy==2.1.3
pandas==2.2.3
scikit-learn==1.5.2
opencv-python==4.10.0.84
Pillow==11.0.0
# YOLO 目标检测模型
ultralytics>=8.0.0
# OpenAI 强大的开源本地语音识别模型
openai-whisper>=20231117

# ==========================================
# 7. 音视频进阶处理 (针对分镜切分与音视频对齐)
# ==========================================
# 用于视频剪辑、音频提取 (已适配 2.x 新版 API)
moviepy==2.2.1
# 轻量级音频处理工具
pydub==0.25.1

# ==========================================
# 8. 单元测试框架
# ==========================================
pytest==8.3.4
pytest-asyncio==0.24.0

# ==========================================
# 9. 异步任务、分布式队列与缓存 (核心新增)
# ==========================================
# 💡 用于高并发点赞、缓存
redis>=5.0.1
# 高性能 Redis 解析器
hiredis>=2.2.0
# 💡 分布式任务队列，用于处理视频识别等长时任务
celery>=5.4.0
# (可选) Celery 任务监控工具
flower>=2.0.1